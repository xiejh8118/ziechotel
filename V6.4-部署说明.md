# ZIEC HOTEL V6.4 部署说明

## Vercel Hobby 免费版特别操作（必须执行）

本修复版已把原来的 12 个 Serverless Functions 合并为 1 个统一函数，网站功能不减少。

GitHub 的“上传文件”只会覆盖同名文件，不会删除旧文件。因此重新上传前，必须先在 GitHub 仓库中删除整个旧 `api` 文件夹，再上传本压缩包的全部内容。上传完成后，仓库内 `api` 文件夹只应有一个文件：`[...path].js`。

如果旧 `api/admin-login.js`、`api/bookings.js` 等文件仍在 GitHub，Vercel仍会把它们分别计算为函数，并继续报超过12个的错误。

1. 将本源码完整上传至 GitHub 仓库并等待 Vercel 部署成功。
2. 在当前线上 Supabase 项目的 SQL Editor 中完整执行 `V6.4-数据库升级.sql`。
3. 确认 Vercel 环境变量 `SUPABASE_URL`、`SUPABASE_SERVICE_ROLE_KEY`、`ADMIN_PASSWORD`、`ADMIN_SESSION_SECRET` 属于同一个 Supabase 项目。
4. 打开 `/hotels.html#booking` 提交一条测试入住需求，页面应返回以 `ZIEC-` 开头的订单编号。
5. 登录 `/admin.html`，在“在线订单”查看并更新该客户状态和跟进备注。
6. 在“视频与咨询设置”填写视频、视频号、YouTube及四个平台账号；启用视频后刷新首页检查。
7. 分别点击 `中｜EN｜KH`，检查电脑和手机页面；底部应显示 `ZIEC HOTEL V6.4`。

如未执行第2步，订单接口会提示数据库字段尚未同步，这是V6.4部署中必须完成的数据库升级。
