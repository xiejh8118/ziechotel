# ZIEC HOTEL V6.6｜全网封面与柬文完整版

发布日期：2026-08-09

## 本版完成

- 不修改原有栏目顺序、页面框架、订单数据库和后台业务逻辑。
- 使用酒店正门实景 `assets/ziec-cover-v66.png` 作为全站统一品牌封面。
- 首页主视觉、酒店主视觉、视频默认封面、社交分享图及结构化数据统一引用新封面。
- 封面保持原始比例，电脑端和手机端使用居中裁切，禁止拉伸变形。
- 扩充 KH 柬文词典，覆盖导航、首页正文、房型、设施、企业服务、供应链、表单、按钮、提示和页脚。
- KH 模式增加遗漏保护，不再回退显示中文原文。
- 保留单一 Serverless Function，不增加 Vercel Hobby 函数数量。
- 保留已验证的订单提交、42501 权限修复及 Supabase 数据结构。
- 全站底部版本号更新为 `ZIEC HOTEL V6.6`。

## 部署

完整替换 GitHub 仓库文件，等待 Vercel 自动部署。此次没有新增数据库字段，无需重新执行 SQL。

部署后重点检查：

1. 首页和酒店页封面为酒店正门实景，图片没有拉伸。
2. 选择 `KH` 后逐页检查，不应再出现中文正文、按钮或表单提示。
3. 测试一次入住需求提交，确认仍能生成 `ZIEC-` 订单编号。
4. 网站底部显示 `ZIEC HOTEL V6.6`。
