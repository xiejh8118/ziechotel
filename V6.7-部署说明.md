# ZIEC HOTEL V6.7 部署说明

## 上传与部署

将压缩包内容完整替换GitHub仓库文件，等待Vercel自动部署。`api`目录仍只有一个`[...path].js`。

## 必须检查的环境变量

- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`
- `ADMIN_PASSWORD`：至少12位，不使用默认密码
- `ADMIN_SESSION_SECRET`：至少32位随机字符串，与后台密码不同

未配置后两项时，V6.7会主动关闭后台登录，防止空密码进入。

## 发布后验收

1. 页脚显示`ZIEC HOTEL V6.7`。
2. 中文、英文、KH逐页打开；KH除品牌名外不残留普通英文。
3. 提交一次入住需求并取得订单编号。
4. 后台使用正确密码登录，错误密码连续尝试会被限制。
5. 在供应商后台新增、删除、排序图片，确认第一张同步成为前台主图。
6. 分享任一供应商，确认预览显示单位名称与公司图片，并进入对应详情。
7. 手机端四个咨询图标保持单行、不重叠。

本版不新增数据库字段，无需执行SQL。
