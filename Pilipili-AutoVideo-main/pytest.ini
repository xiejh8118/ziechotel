[pytest]
markers =
    api: Tests that require real API keys (deselect with '-m "not api"')
    e2e: End-to-end tests that run the full pipeline (deselect with '-m "not e2e"')
    slow: Tests that take a long time to run
asyncio_mode = auto
