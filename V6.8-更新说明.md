# ZIEC HOTEL V6.8——客户订单打印与导出版

## 本版已完成

- 后台“在线订单”升级为“客户订单管理”。
- 单笔订单支持查看、编辑、A4打印及保存PDF。
- 打印内容包含订单编号、客户资料、入住日期、房型、房数、接送、来源、状态和备注。
- 打印页面显示Logo、网站地址与打印时间，并自动隐藏操作按钮。
- 手机可保存PDF后分享到微信、Telegram、Messenger或WhatsApp。
- 支持全部订单一键导出Excel（`.xls`）。
- 增加“未打印 / 已打印次数”标记。
- 原订单提交接口与单Serverless Function结构保持不变。

## 部署后执行一次

在 Supabase → SQL Editor 执行 `V6.8-订单打印标记数据库升级.sql`。脚本只新增打印时间与打印次数两个字段，不删除旧订单。

## 使用

进入后台 → 在线订单。“查看/编辑”可修改保存；“打印 / PDF”生成A4订单；“批量导出Excel”下载全部订单。
